<ul id="toggles">
    <% @toggles.each do |toggle| %>
        <li id="<%= toggle.name %>">
            <div><%= toggle.display_name %></div>
            <div class="toggle <%= toggle.value ? "on" : "off" %>"><%= toggle.value %></div>
        </li>
    <% end %>
</ul>